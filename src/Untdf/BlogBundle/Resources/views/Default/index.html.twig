<!DOCTYPE html>
<html>
 <head>
 <h1>
	Listado de articulos 
 </head>
 <body>
   {# dump(posts) #}
   <ul> 
   {% for post in posts %}
   <li>
    <a href="{{ path('show_post', {'id': post.id}) }}">
     {{post.title}}
    </a>
   </li>
  {% endfor %}
  </ul>
 </body>
</html>  
